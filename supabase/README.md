# Supabase Infrastructure (Phase 0 Placeholder)

Supabase integration intentionally not implemented in Phase 0.

This directory will hold:
- `migrations/`: SQL migrations (applied in subsequent phases)
- `seed/`: Initial seed data
- `config/`: Supabase CLI and local configurations

Per project roadmap, all database schema, tables, Auth, and RLS policies will be introduced in later phases following approval from the Team Leader.
